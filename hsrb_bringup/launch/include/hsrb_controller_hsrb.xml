<?xml version="1.0"?>
<launch>
  <arg name="namespace" default="/hsrb" />
  <arg name="robot_name" default="hsrb" />

  <group ns="$(arg namespace)">

    <!-- controller -->
    <node name="controller_spawner" pkg="controller_manager" type="spawner"
          respawn="false"
          output="screen"
          args="--namespace=$(arg namespace)
                joint_state_controller
                servo_state_controller
                omni_base_controller
                head_trajectory_controller
                arm_trajectory_controller
                drive_mode_controller
                gripper_controller
                servo_parameter_reader
                servo_parameter_writer
                imu_sensor_controller
                collision_detection_controller
                tmc_diagnostic_controller
                --shutdown-timeout 1" />

    <rosparam command="load" file="$(find hsrb_bringup)/params/hsrb_hw_config_$(arg robot_name).yaml" />
  </group>
</launch>
