[Unit]
Description=AMIGO start.launch

[Install]
WantedBy=multi-user.target

[Service]
Restart=on-abort
ExecStart=/usr/bin/sudo -H -u amigo bash -c 'source /home/amigo/.tue/setup.bash && roslaunch amigo_bringup start.launch --wait'
