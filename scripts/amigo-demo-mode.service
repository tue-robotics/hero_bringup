[Unit]
Description=AMIGO demo_mode.launch

[Install]
WantedBy=multi-user.target

[Service]
Restart=no
ExecStart=/usr/bin/sudo -H -u amigo bash -c 'source /home/amigo/.tue/setup.bash && rosnode kill /state_machine && roslaunch amigo_bringup demo_mode.launch --wait'
